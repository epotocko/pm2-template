{
  "name": "pm2-test",
  "version": "0.1.0",
  "description": "PM2 test server",
  "private": true,
  "main": "app.js",
  "scripts": {
    "test": "mocha --reporter spec --bail --timeout 5000 --check-leaks test/",
    "start": "node app.js"
  },
  "license": "BSD",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.3"
  },
  "devDependencies": {
    "form-urlencoded": "^3.0.0",
    "mocha": "*",
    "supertest": "^3.3.0"
  }
}
